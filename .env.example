# --- Application ---
PORT=3000
SESSION_SECRET=change-me
NODE_ENV=development
COOKIE_SECURE=false

# --- Database (shared with Docker Compose) ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=sysadmin_users
POSTGRES_PORT=5432

# --- Local-only connection string (when running `node index.js` on host) ---
DATABASE_URL=postgres://postgres:postgres@localhost:5432/sysadmin_users
